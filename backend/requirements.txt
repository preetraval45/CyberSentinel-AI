# Core Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
# psycopg2-binary==2.9.10  # Actually installed (newer version than specified due to Python 3.13 compatibility)
psycopg2-binary==2.9.9  # Specified version may not install on Python 3.13, newer version installed instead
alembic==1.12.1
# pydantic==2.11.7  # Actually installed (newer version than specified due to Rust compilation requirements)
pydantic==2.5.0  # Specified version may not install due to Rust compilation requirements, newer version installed instead
pydantic[email]==2.5.0
python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4  # Actually installed (different extras than specified)
passlib[argon2]==1.7.4  # Specified version with argon2 extras, but bcrypt extras also work
argon2-cffi==23.1.0
python-multipart==0.0.6
redis==5.0.1
openai==0.28.1
qrcode[pil]==7.4.2
httpx==0.25.2

# Development Dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
# pytest-cov==6.2.1  # Actually installed (newer version than specified)
pytest-cov==4.1.0  # Specified version was successfully installed

# Notes:
# 1. The actual installed versions may differ from those specified due to compatibility issues with Python 3.13
# 2. psycopg2-binary 2.9.10 was installed instead of 2.9.9 due to compilation issues with Python 3.13
# 3. pydantic 2.11.7 was installed instead of 2.5.0 due to Rust compilation requirements
# 4. passlib with bcrypt extras was installed instead of argon2 extras, but both work for password hashing
# 5. pytest-cov 6.2.1 was initially installed, but 4.1.0 was successfully installed to match requirements
